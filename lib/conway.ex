defmodule Conway do
  #use Application

  #def start(_type,_args) do
      #board = Board.create_board(10,10)
      #IO.puts Board.print_board(board)
      
      #Game.run(board,100)
      #{:ok}
  #end  
end
